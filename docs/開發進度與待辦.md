# BaF Google Sheet 同步 — 開發進度與待辦（交接筆記）

> 給接手的人/新對話：先讀這份，再看下面兩個主檔。本功能在 pyRevit 外掛 BaF.extension。

## 主要檔案
- Revit 端（IronPython 2.7）：
  `Tools.tab/Views.panel/BatchManageSheets.pushbutton/script.py`
  工具顯示名稱「圖紙編輯與管理」，同步功能在第 5 個分頁「Google Sheet 同步」。
- Google 端（Apps Script）：`docs/gsheet_sync_appscript.gs`
  版本看程式裡的 `apiVersion`（會顯示在匯出訊息）。更新方式：
  工具「📄 最新腳本」開檔複製 → Apps Script 貼上覆蓋存檔 → **新增/編輯部署作業（新版本）**。
- 使用說明：`docs/使用說明_從零設定.txt`
- 本機設定（URL/頁籤，不進 git）：`%APPDATA%\pyRevit\baf_gsheet_config.json`

## Git
- 分支 `feat/gsheet-sync`，**尚未 push**（同事 repo: b00204002-spec/BaF.extension）。
- 改完一定 `py -m py_compile` 檢查 script.py；.gs 用 `node --check`(複製成 .js)。

## 已完成（核心雙向同步，已測過）
- 匯出 Revit→Sheet：認表頭名稱寫入、空白頁籤自動建表頭(從A欄、只寫值不套色)、
  自動移除表頭左側空白欄、狀態欄核取方塊、繪圖員下拉、鎖欄位表頭列、凍結表頭、
  更新時間(yyyyMMdd，HH:mm)。**匯出前有「預覽差異→確認匯出」**。
- 依「圖紙類別」拆成多個分表(以類別命名)；分表整頁乾淨重建、上方有「批次參數」區。
- 匯入 Sheet→Revit：**總表=全域**，**分表=鎖定該圖紙類別**(可在該類別內增/改/刪)。
  預覽差異→確認執行→在 main() 關窗後才改模型(避免被還原)→自動回寫總表+重新拆分。
  圖號重複會擋下；刪除前列清單確認；新增自動回寫 UID。
- 分表標頭「批次參數」(審圖員/設計者/批准者/圖紙發布日期/校核2)匯入時套到該類別所有圖。
- 角色標記(總表/附表 用 DeveloperMetadata)；分表編輯 onEdit 即時回寫總表。
- 開新管理器前會關掉上一個視窗(AppDomain)。

## Revit 端需要的圖紙文字參數(類別=圖紙/Sheets)
圖紙類別、繪圖員、修正備註、審圖員、設計者、批准者、圖紙發布日期、校核2
（沒有的會自動略過、不報錯）

## 建築師實測回饋修正（2026-06，已實作待實測；py_compile 已過）
1. 匯入遇重複圖號 → 跳 `_DupResolveWindow` 就地改圖號(不再只丟訊息叫人自己改)。
   改完直接更新 records；`_on_import_preview` 設 `plan["resolvedDup"]`，
   main() 匯入後會自動回寫 Google Sheet(讓表上重複也一起修正)。
2. 匯出防呆：`_on_write_gsheet` 匯出前讀回目標頁籤，若 `role=="sub"`(分表)就擋下，
   提示改用總表頁籤名稱(分表由「依類別拆分」自動更新)。
3. 刪除/重建確認改用 `_ListConfirmWindow`：列「全部」將刪/重建圖紙、右側有捲軸
   (不再只顯示前 30 筆)。
4. 用語改名(全 UI 與報告)：「預留」→「僅文字資訊」、「真實」→「真實圖紙」。
5. 🛠️ 建立環境升級(見下)。輔助視窗類別 `_brush`/`_ListConfirmWindow`/
   `_DupResolveWindow` 放在主類別與 create_sheets 之間。
6. 既存參數偵測修正：原本用 `doc.ParameterBindings` → 抓不到「內建」圖紙參數
   (繪圖員/審圖員/設計者/批准者/圖紙發布日期 是 Revit 內建 Drawn/Checked/
   Designed/Approved By、Sheet Issue Date)，會誤判成缺而想重建。改成
   `_existing_sheet_param_names(wanted)` 用真實圖紙判斷；空專案無圖紙才退回
   ParameterBindings。
7. 內建參數用字差異(佈 vs 布)修正：公司範本內建是「圖紙發佈日期(佈)」，我們同步
   用「圖紙發布日期(布)」，名稱比對不到 → 誤建空白重複參數。改用模組層
   `BUILTIN_PARAM_MAP`(邏輯名→BuiltInParameter：SHEET_DRAWN/CHECKED/DESIGNED/
   APPROVED_BY、SHEET_ISSUE_DATE) + `_lookup_sheet_param()`：對照表內名稱**一律
   優先用 BuiltInParameter**(避開同名誤建參數)，其餘用 LookupParameter。
   `_read_text_param`/`_set_param`/偵測/明細表欄位(用 SchedulableField.ParameterId
   對應)全部走這個解析。→ 同步會正確讀寫內建那張(佈)的值。
   ⚠ 先前誤建的空白「圖紙發布日期(布)」專案參數可請建築師手動刪除(管理→專案參數)，
   不刪也不影響同步(已優先讀內建)。
8. 圖紙明細表建立失敗修正：圖紙清單必須用 `ViewSchedule.CreateSheetList(document)`，
   原本用 `CreateSchedule(doc, OST_Sheets)` 會丟例外→被接住變「建立失敗」。
   已改 CreateSheetList(失敗才退回 CreateSchedule)，並用 `GetFieldCount/GetField`
   記錄預設已帶欄位(圖號/圖名)避免重複加。名稱=「BaF_Sheet專用圖紙明細表」。
   重跑建立環境(參數已全在時會走「只建明細表」分支)即可補建。

## 待辦（佔位按鈕已建，功能未做）
1. 🛠️ 建立環境 `_on_build_env`：一鍵建立 8 個圖紙參數(缺的補建) + 圖紙明細表。
   — ✅ **已實作（待 Revit 內實測）**。流程：handler 算缺哪些(用
     `_sheet_bound_param_names()` 偵測已綁到 OST_Sheets 的參數)→列清單確認→關窗，
     main() 走 `build_env()`。**公司共用參數優先**：先查公司共用參數檔
     (`COMPANY_SP_DIR`=\\data.bioarch.com.tw\...\03 共用參數，取最新 .txt)，
     檔內已有的名稱→沿用共用參數定義(保留 GUID，目前只有「圖紙類別」)；
     其餘→用 `%TEMP%\BaF_shared_params.txt` 暫存檔建為專案參數。
     兩者都 `NewInstanceBinding` 綁 OST_Sheets、交易內 `Insert`(3參數PG_TEXT 退2參數)。
     完成後 `_ensure_sheet_schedule()` 建「圖紙明細表」(同名已存在則略過；欄位=圖號/
     圖名+8參數)。**不碰 Google Sheet**。還原原本 SharedParametersFilename。
     跨版本：`_text_spec()` 試 `SpecTypeId.String.Text` 退 `ParameterType.Text`。
     ⚠ 公司共用參數資料夾有「不要改動內容」捷徑→只讀不寫。
     待測：各版本 Revit 開→同步分頁→🛠️建立環境，確認(a)圖紙類別走共用參數、
     其餘走專案參數且都出現在圖紙屬性可填值；(b)「圖紙明細表」有建立且欄位齊；
     (c)重跑會略過已存在參數/明細表。
2. 指派工作任務 `_on_assign_tasks`：把各圖「修正備註」依「繪圖員」分組成每人待辦。
   — 輸出去向待定(Google Sheet 分頁 / Google 文件 / Slack)。難度中。
3. 👁 依會議紀錄編輯圖紙清單 `_on_edit_by_minutes`：貼上會議紀錄→Claude API 抓出
   每張圖待辦→預覽→寫進「修正備註」。需接 Claude API、決定金鑰存放/比對方式(建議圖號優先)。
   難度高、最符合會議願景、建議壓軸。

## 同步資料流速記
- 匯出 records 含 7 欄 + 5 批次參數(供拆分填標頭)。
- 匯入 readSheet 回傳 records/role/category/batchParams/mainTab。
  role=sub → Revit 端 _build_plan(scope_category, batch) 鎖類別 + 批次參數併入。
- 套用在 _apply_plan(交易在 main() 內)；預覽用 _plan_to_rows(與套用同一份計畫)。
