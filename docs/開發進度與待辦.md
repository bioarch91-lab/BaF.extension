# BaF Google Sheet 同步 — 開發進度與待辦（交接筆記）

> 給接手的人/新對話：先讀這份，再看下面兩個主檔。本功能在 pyRevit 外掛 BaF.extension。

## 主要檔案
- Revit 端（IronPython 2.7）：
  `Tools.tab/Views.panel/BatchManageSheets.pushbutton/script.py`
  工具顯示名稱「圖紙編輯與管理」，同步功能在第 5 個分頁「Google Sheet 同步」。
- Google 端（Apps Script）：`docs/gsheet_sync_appscript.gs`
  版本看程式裡的 `apiVersion`（會顯示在匯出訊息）。更新方式：
  工具「📄 最新腳本」開檔複製 → Apps Script 貼上覆蓋存檔 → **新增/編輯部署作業（新版本）**。
- 使用說明：`docs/使用說明_從零設定.txt`
- 本機設定（URL/頁籤，不進 git）：`%APPDATA%\pyRevit\baf_gsheet_config.json`

## Git
- 分支 `feat/gsheet-sync`，**尚未 push**（同事 repo: b00204002-spec/BaF.extension）。
- 改完一定 `py -m py_compile` 檢查 script.py；.gs 用 `node --check`(複製成 .js)。

## 已完成（核心雙向同步，已測過）
- 匯出 Revit→Sheet：認表頭名稱寫入、空白頁籤自動建表頭(從A欄、只寫值不套色)、
  自動移除表頭左側空白欄、狀態欄核取方塊、繪圖員下拉、鎖欄位表頭列、凍結表頭、
  更新時間(yyyyMMdd，HH:mm)。**匯出前有「預覽差異→確認匯出」**。
- 依「圖紙類別」拆成多個分表(以類別命名)；分表整頁乾淨重建、上方有「批次參數」區。
- 匯入 Sheet→Revit：**總表=全域**，**分表=鎖定該圖紙類別**(可在該類別內增/改/刪)。
  預覽差異→確認執行→在 main() 關窗後才改模型(避免被還原)→自動回寫總表+重新拆分。
  圖號重複會擋下；刪除前列清單確認；新增自動回寫 UID。
- 分表標頭「批次參數」(審圖員/設計者/批准者/圖紙發布日期/校核2)匯入時套到該類別所有圖。
- 角色標記(總表/附表 用 DeveloperMetadata)；分表編輯 onEdit 即時回寫總表。
- 開新管理器前會關掉上一個視窗(AppDomain)。

## Revit 端需要的圖紙文字參數(類別=圖紙/Sheets)
圖紙類別、繪圖員、修正備註、審圖員、設計者、批准者、圖紙發布日期、校核2
（沒有的會自動略過、不報錯）

## 待辦（佔位按鈕已建，功能未做）
1. 🛠️ 建立環境 `_on_build_env`：一鍵自動建立上面 8 個圖紙參數(缺的補建)。
   — Revit API 可程式化建專案參數(需共用參數檔綁定 Sheets)。難度中、價值高、建議先做。
2. 指派工作任務 `_on_assign_tasks`：把各圖「修正備註」依「繪圖員」分組成每人待辦。
   — 輸出去向待定(Google Sheet 分頁 / Google 文件 / Slack)。難度中。
3. 👁 依會議紀錄編輯圖紙清單 `_on_edit_by_minutes`：貼上會議紀錄→Claude API 抓出
   每張圖待辦→預覽→寫進「修正備註」。需接 Claude API、決定金鑰存放/比對方式(建議圖號優先)。
   難度高、最符合會議願景、建議壓軸。

## 同步資料流速記
- 匯出 records 含 7 欄 + 5 批次參數(供拆分填標頭)。
- 匯入 readSheet 回傳 records/role/category/batchParams/mainTab。
  role=sub → Revit 端 _build_plan(scope_category, batch) 鎖類別 + 批次參數併入。
- 套用在 _apply_plan(交易在 main() 內)；預覽用 _plan_to_rows(與套用同一份計畫)。
